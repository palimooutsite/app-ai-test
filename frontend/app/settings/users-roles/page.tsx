import { PageShell } from '../../../components/page-shell';

export default function UsersRolesPage() {
  return <PageShell title="Users & Roles" description="Admin area for role assignments (ADMIN, ACCOUNTANT, OWNER) and tenant access." />;
}

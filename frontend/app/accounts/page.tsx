import { PageShell } from '../../components/page-shell';

export default function AccountsPage() {
  return <PageShell title="Chart of Accounts" description="Manage account codes, names, and normal balances from /api/v1/accounts." />;
}

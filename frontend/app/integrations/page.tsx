import { PageShell } from '../../components/page-shell';

export default function IntegrationsPage() {
  return <PageShell title="Integrations" description="Trigger bank and e-commerce sync workflows under /api/v1/integrations." />;
}
